# Copy to .env (or export in your shell) and fill in your key:
#   source .env
# Then run: GEMINI_API_KEY="..." pnpm tauri dev
# The env var takes precedence over any key stored via Settings.
GEMINI_API_KEY=